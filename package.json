{
  "name": "labeler-vc500w-jstests",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only harness for testing the web UI's browser JS. Not shipped, not part of the Python package — see tests/test_store.mjs.",
  "scripts": {
    "test": "node --test tests/test_store.mjs && node --check src/labeler/web/static/app.js && node --check src/labeler/web/static/store.js"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0"
  }
}
